
function slasher(arr, howMany) {
  // it doesn't always pay to be first
  
  var remaining = arr.slice(howMany);
  
  return remaining;
}

slasher([1, 2, 3], 3);


//This one was a no brainer!
//I thought there must be something tricky within the problem.
//But, there's nothing! :D